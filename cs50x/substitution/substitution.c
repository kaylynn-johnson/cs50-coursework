#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    // check for valid number of arguments

    // check for valid key
    // length must be 26 (magic number)
    // each letter should only be used once (move forwards and backwards)
}
