#include <cs50.h>
#include <ctype.h>
#include <stdio.h>

int main(void)
{
    // Ask for text

    // Outer for loop for the 100 word pieces
    // Inner for loop for counting the letters & sentences per section

    // Add value from inner loop to outer loop

    // Calculate average for both based on number of times the outer loop ran

    // Run the equation

    // Print out the appropriate grade level

    // How would we handle the last bit that is less than 100 words?
}
